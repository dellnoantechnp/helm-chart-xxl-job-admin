{{- $canalServerCmName := include "canal.server.configmap.fullname" . -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $canalServerCmName }}
data:
{{- range .Values.InstanceConf.canal.instance }}
  example: |
{{ include "instance.configmap.tpl" . | indent 4 -}}
{{- end }}