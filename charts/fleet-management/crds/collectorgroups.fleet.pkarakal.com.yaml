apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: collectorgroups.fleet.pkarakal.com
spec:
  group: fleet.pkarakal.com
  names:
    kind: CollectorGroup
    listKind: CollectorGroupList
    plural: collectorgroups
    singular: collectorgroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CollectorGroup is the Schema for the collectorgroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CollectorGroup
            properties:
              description:
                description: |-
                  Human-readable description of what this group represents
                  e.g. "Production tenants running the core streaming pipeline"
                type: string
            type: object
          status:
            description: status defines the observed state of CollectorGroup
            properties:
              activeBindings:
                description: |-
                  ActiveBindings is the number of CollectorGroupBindings currently
                  referencing this CollectorGroup. Check this field before deleting
                  the group to understand which bindings would be left dangling.
                type: integer
              observedGeneration:
                format: int64
                type: integer
            required:
            - activeBindings
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
