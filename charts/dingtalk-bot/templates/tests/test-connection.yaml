apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dingtalk-bot.fullname" . }}-test-connection"
  labels:
    {{- include "dingtalk-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dingtalk-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
